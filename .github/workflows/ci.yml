name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

#env:
#  ACR_LOGIN_SERVER: acraiacademy26.azurecr.io

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "24"
      - run: npm ci
      - run: npm run lint
      - run: npm test
      - name: Build Docker image
#        env:
#          IMAGE_TAG: git-happens-backend:${{ github.sha }}
#        run: docker build --tag "$IMAGE_TAG" .

#      - name: Save Docker image
#        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
#        run: docker save git-happens-backend:${{ github.sha }} | gzip > git-happens-backend.tar.gz

#      - name: Upload Docker image
#        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
#        uses: actions/upload-artifact@v4
#        with:
#          name: git-happens-backend-image
#          path: git-happens-backend.tar.gz

#  push:
#    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
#    needs: test
#    runs-on: ubuntu-latest
#    steps:
#      - name: Download Docker image
#        uses: actions/download-artifact@v4
#        with:
#          name: git-happens-backend-image

#      - name: Load Docker image
#        run: gunzip -c git-happens-backend.tar.gz | docker load

#      - name: Log in to Azure Container Registry
#        uses: docker/login-action@v3
#        with:
#          registry: ${{ env.ACR_LOGIN_SERVER }}
#          username: ${{ secrets.AZURE_CLIENT_ID }}
#          password: ${{ secrets.AZURE_CLIENT_SECRET }}
#      - name: Push Docker image
#        env:
#          IMAGE: ${{ env.ACR_LOGIN_SERVER }}/git-happens-backend
#          LOCAL_IMAGE: git-happens-backend:${{ github.sha }}
#          SHA_TAG: ${{ env.ACR_LOGIN_SERVER }}/git-happens-backend:${{ github.sha }}
#        run: |
#          docker tag "$LOCAL_IMAGE" "$SHA_TAG"
#          docker push "$SHA_TAG"
#          docker tag "$SHA_TAG" "$IMAGE:latest"
#          docker push "$IMAGE:latest"
#      - name: Verify images in Azure Container Registry
#        env:
#          IMAGE_SHA: ${{ github.sha }}
#        run: |
#          docker manifest inspect "$ACR_LOGIN_SERVER/git-happens-backend:$IMAGE_SHA"
#          docker manifest inspect "$ACR_LOGIN_SERVER/git-happens-backend:latest"

#  terraform-plan:
#    name: Terraform plan
#    runs-on: ubuntu-latest
#    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
#    env:
#      ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
#      ARM_CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}
#      ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
#      ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
#      ARM_USE_AZUREAD: true
#      TF_VAR_backend_image: acraiacademy26.azurecr.io/git-happens-backend:${{ github.sha }}
#      TF_VAR_environment: dev
#    defaults:
#      run:
#        working-directory: infastructure
#    steps:
#      - uses: actions/checkout@v4
#      - uses: hashicorp/setup-terraform@v3
#      - name: Check Terraform formatting
#        run: terraform fmt -check -recursive
#      - name: Initialize Terraform remote state
#        run: terraform init -input=false -backend-config=backend.hcl
#      - name: Validate Terraform
#        run: terraform validate
#      - name: Plan Terraform changes
#        run: terraform plan -input=false -no-color

#  terraform-deploy:
#    name: Terraform apply
#    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
#    needs: push
#    runs-on: ubuntu-latest
#    concurrency:
#      group: terraform-dev
#    env:
#      ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
#      ARM_CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}
#      ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
#      ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
#      ARM_USE_AZUREAD: true
#      TF_VAR_environment: dev
#      TF_VAR_backend_image: acraiacademy26.azurecr.io/git-happens-backend:${{ github.sha }}
#    defaults:
#      run:
#        working-directory: infastructure
#    steps:
#      - uses: actions/checkout@v4
#      - uses: hashicorp/setup-terraform@v3
#      - name: Initialize Terraform remote state
#        run: terraform init -input=false -backend-config=backend.hcl
#      - name: Validate Terraform
#        run: terraform validate
#      - name: Plan Terraform changes
#        run: terraform plan -input=false -no-color -out=tfplan
#      - name: Apply Terraform changes
#        run: terraform apply -input=false -auto-approve tfplan

#  database-migrate:
#    name: Apply Prisma migrations
#    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
#    needs: terraform-deploy
#    runs-on: ubuntu-latest
#    env:
#      KEY_VAULT_NAME: githappensdev14454c
#      POSTGRES_SERVER_NAME: git-happens-dev-pg
#      POSTGRES_RESOURCE_GROUP: git-happens-dev
#      POSTGRES_FIREWALL_RULE_NAME: github-actions-runner
#    steps:
#      - uses: actions/checkout@v4
#      - uses: actions/setup-node@v4
#        with:
#          node-version: "24"
#          cache: npm
#      - name: Install dependencies
#        run: npm ci
#        uses: azure/login@v2
#        with:
#      - name: Log in to Azure
#          creds: '{"clientId":"${{ secrets.AZURE_CLIENT_ID }}","clientSecret":"${{ secrets.AZURE_CLIENT_SECRET }}","subscriptionId":"${{ secrets.AZURE_SUBSCRIPTION_ID }}","tenantId":"${{ secrets.AZURE_TENANT_ID }}"}'
#      - name: Allow this runner through the Postgres firewall
#        run: |
#          RUNNER_IP="$(curl -s https://api.ipify.org)"
#          echo "RUNNER_IP=$RUNNER_IP" >> "$GITHUB_ENV"
#          az postgres flexible-server firewall-rule create \
#            --resource-group "$POSTGRES_RESOURCE_GROUP" \
#            --server-name "$POSTGRES_SERVER_NAME" \
#            --name "$POSTGRES_FIREWALL_RULE_NAME" \
#            --start-ip-address "$RUNNER_IP" \
#            --end-ip-address "$RUNNER_IP"
#      - name: Apply pending Prisma migrations
#        run: |
#          DATABASE_URL="$(az keyvault secret show \
#            --vault-name "$KEY_VAULT_NAME" \
#            --name database-url \
#            --query value \
#            --output tsv)" npx prisma migrate deploy
#      - name: Remove runner rule from the Postgres firewall
#        if: always()
#        run: |
#          az postgres flexible-server firewall-rule delete \
#            --resource-group "$POSTGRES_RESOURCE_GROUP" \
#            --server-name "$POSTGRES_SERVER_NAME" \
#            --name "$POSTGRES_FIREWALL_RULE_NAME" \
#            --yes
